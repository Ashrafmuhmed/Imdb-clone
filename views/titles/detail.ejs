<%- include('../includes/head.ejs', {
    pageTitle: (typeof title !== 'undefined' ? title.primary_title : 'Title') + ' - IMDB Clone',
    showNav: true,
    isLoggedIn: typeof isLoggedIn !== 'undefined' ? isLoggedIn : false,
    user: typeof user !== 'undefined' ? user : null
}) %>

<main class="title-detail-container">
    <div class="container">
        <% if (typeof title !== 'undefined') { %>
        <div class="title-detail-header">
            <div class="title-detail-info">
                <h1 class="title-detail-name"><%= title.primary_title %></h1>
                <% if (title.original_title && title.original_title !== title.primary_title) { %>
                    <p class="title-original"><%= title.original_title %></p>
                <% } %>
                
                <div class="title-stats">
                    <div class="stat-item">
                        <span class="stat-label">Rating:</span>
                        <span class="stat-value rating-highlight">
                            <% if (title.rating && title.rating.average_rating) { %>
                                <%= title.rating.average_rating %> ⭐
                            <% } else { %>
                                N/A ⭐
                            <% } %>
                        </span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">Votes:</span>
                        <span class="stat-value">
                            <% if (title.rating && title.rating.num_votes) { %>
                                <%= title.rating.num_votes.toLocaleString() %>
                            <% } else { %>
                                N/A
                            <% } %>
                        </span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">Type:</span>
                        <span class="stat-value"><%= title.title_type || 'N/A' %></span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">Year:</span>
                        <span class="stat-value"><%= title.start_year || 'N/A' %><% if (title.end_year) { %> - <%= title.end_year %><% } %></span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">Runtime:</span>
                        <span class="stat-value"><%= title.runtime_minutes || 'N/A' %> minutes</span>
                    </div>
                </div>

                <% if (typeof genres !== 'undefined' && genres.length > 0) { %>
                <div class="title-genres">
                    <% genres.forEach(genre => { %>
                        <span class="genre-tag"><%= genre.name %></span>
                    <% }); %>
                </div>
                <% } %>

                <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                <div class="title-actions">
                    <form action="/watchlist/add" method="POST" class="watchlist-form">
                        <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                        <input type="hidden" name="tconst" value="<%= title.tconst %>">
                        <button type="submit" class="watchlist-btn">+ Add to Watchlist</button>
                    </form>
                </div>
                <% } %>
            </div>
        </div>

        <div class="title-detail-content">
            <% if (typeof directors !== 'undefined' && directors.length > 0) { %>
            <section class="detail-section" id="directors">
                <h2>Directors</h2>
                <div class="people-list">
                    <% directors.forEach(director => { %>
                    <a href="/names/<%= director.nconst %>" class="person-card">
                        <div class="person-name"><%= director.primary_name %></div>
                    </a>
                    <% }); %>
                </div>
                <% if (typeof hasMoreDirectors !== 'undefined' && hasMoreDirectors && !showAll) { %>
                    <a href="?showAll=true#directors" class="show-all-link">Show all directors (<%= typeof allDirectors !== 'undefined' ? allDirectors.length : directors.length %>)</a>
                <% } %>
            </section>
            <% } %>

            <% if (typeof writers !== 'undefined' && writers.length > 0) { %>
            <section class="detail-section" id="writers">
                <h2>Writers</h2>
                <div class="people-list">
                    <% writers.forEach(writer => { %>
                    <a href="/names/<%= writer.nconst %>" class="person-card">
                        <div class="person-name"><%= writer.primary_name %></div>
                    </a>
                    <% }); %>
                </div>
                <% if (typeof hasMoreWriters !== 'undefined' && hasMoreWriters && !showAll) { %>
                    <a href="?showAll=true#writers" class="show-all-link">Show all writers (<%= typeof allWriters !== 'undefined' ? allWriters.length : writers.length %>)</a>
                <% } %>
            </section>
            <% } %>

            <% if (typeof principals !== 'undefined' && principals.length > 0) { %>
            <section class="detail-section" id="cast-crew">
                <h2>Cast & Crew</h2>
                <div class="principals-list">
                    <% 
                    // Group principals by category and merge actor/actress into cast
                    const groupedPrincipals = {};
                    principals.forEach(p => {
                        let cat = (p.title_principals && p.title_principals.category) || (p.category || 'other');
                        // Merge actor and actress into cast
                        if (cat === 'actor' || cat === 'actress') {
                            cat = 'Cast';
                        }
                        if (!groupedPrincipals[cat]) {
                            groupedPrincipals[cat] = [];
                        }
                        groupedPrincipals[cat].push(p);
                    });
                    %>
                    <% Object.keys(groupedPrincipals).forEach(category => { %>
                        <div class="principals-category">
                            <h3 class="category-title"><%= category.charAt(0).toUpperCase() + category.slice(1) %></h3>
                            <div class="principals-items">
                                <% groupedPrincipals[category].forEach(principal => { %>
                                <div class="principal-item">
                                    <div class="principal-card">
                                        <div class="principal-header">
                                            <a href="/names/<%= principal.nconst %>" class="principal-name-link">
                                                <div class="principal-name"><%= principal.primary_name || 'Unknown' %></div>
                                            </a>
                                        </div>
                                        
                                        
                                        <% const characters = principal.title_principals && principal.title_principals.characters ? principal.title_principals.characters : (principal.characters || null); %>
                                        <% if (characters && characters !== '\\N' && characters !== null) { %>
                                            <div class="principal-character">
                                                <span class="info-icon">🎭</span>
                                                <span class="character-label">Character:</span>
                                                <span class="character-name"><%= characters %></span>
                                            </div>
                                        <% } %>
                                        
                                        <% const job = principal.title_principals && principal.title_principals.job ? principal.title_principals.job : (principal.job || null); %>
                                        <% if (job && job !== '\\N' && job !== null) { %>
                                            <div class="principal-job">
                                                <span class="info-icon">💼</span>
                                                <span class="job-label">Job:</span>
                                                <span class="job-name"><%= job %></span>
                                            </div>
                                        <% } %>
                                        
                                        <a href="/names/<%= principal.nconst %>" class="principal-view-profile">
                                            View Full Profile →
                                        </a>
                                    </div>
                                </div>
                                <% }); %>
                            </div>
                        </div>
                    <% }); %>
                </div>
                <% if (typeof hasMorePrincipals !== 'undefined' && hasMorePrincipals && !showAll) { %>
                    <a href="?showAll=true#cast-crew" class="show-all-link">Show all cast & crew (<%= typeof allPrincipals !== 'undefined' ? allPrincipals.length : principals.length %>)</a>
                <% } %>
            </section>
            <% } %>

            <% if (typeof episodes !== 'undefined' && episodes.length > 0) { %>
            <section class="detail-section">
                <h2>Episodes</h2>
                <div class="episodes-list">
                    <% episodes.forEach(episode => { %>
                    <div class="episode-card">
                        <div class="episode-header">
                            <div class="episode-number">S<%= episode.season_number %>E<%= episode.episode_number %></div>
                            <a href="/titles/<%= episode.episode_title.tconst %>" class="episode-link">
                            <% if( episode.episode_title.primary_title ) { %> 
                            <div class="episode-title"><%= episode.episode_title.primary_title %></div>
                            <% } else { %>
                                <div class="episode-info">
                                <h3>Episode <%= episode.episode_number %></h3>
                                <p class="episode-season">Season <%= episode.season_number %></p>
                            </div>
                            <% } %>
                            </a>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </section>
            <% } %>
        </div>
        <% } else { %>
            <div class="error-message">
                <h2>Title not found</h2>
                <p>The title you're looking for doesn't exist.</p>
                <a href="/titles" class="back-link">Browse Titles</a>
            </div>
        <% } %>
    </div>
</main>

<%- include('../includes/end.ejs') %>

